# Task 1
## a)
$$
\begin{gathered}
\lambda = \frac{v}{f} \\
\lambda_{L_1} = \frac{3 \times 10^8 \ \mathrm{m/s}}{1.57542 \times 10^9 \ \mathrm{s}} \approx 0.1904 \ \mathrm{m} = 19.04 \ \mathrm{cm} \\
\lambda_{L_2} = \frac{3 \times 10^8 \ \mathrm{m/s}}{1.22760 \times 10^9 \ \mathrm{s}} \approx 0.2444 \ \mathrm{m} = 24.44 \ \mathrm{cm}
\end{gathered}
$$
#### Full Cycles:
$$
\begin{gathered}
\lambda_{L_1} = 106\ 087\ 990 \\
\lambda_{L_2} = 82\ 647\ 927
\end{gathered}
$$

## b)
#### Each chip:
$\frac{1}{f_{C\/A}} = \frac{1}{1.023 \times 10^6} = 9.7752 \times 10^{-7} \mathrm{s} = 0.9775 \ \mathrm{\micro s} $
#### Entire Code:
$0.9775 \times 1023 = 999.9825 \ \mathrm{\micro s} \approx 1 \mathrm{ms} $

## c)
$\frac{f_{L_1}}{f_{C/A}} = \frac{1.57542 \times 10^9}{1.023 \times 10^6} = 1540 \implies \lambda_{C/A} = 1540 \ \lambda_{L_{1}} $

## d)
### From page 90 of **IS_GPS_200J.pdf**:
> ... the message structure shall utilize a basic format of a 1500 bit long frame made up of
five subframes, each subframe being 300 bits long. Subframes 4 and 5 shall be subcommutated 25 times each,
**so that a complete data message shall require the transmission of 25 full frames**.

#### For each 1500-bit frame:
$\frac{1500}{50 \mathrm{Hz}} = 30 \ \mathrm{s} $
#### Complete data message:
$\frac{25 \times 30 }{60} = 12.5 \mathrm{minutes} $

### Minimum navigation data:
#### From lecture notes page 31:
> To use the satellite for positioning, only the data from subframes 1, 2, and 3 are required; these are repeated every 30 sec.
For the prediction of satellite visibility, satellite geometrical distribution, etc.
Subframe 4 & 5 data is required.

#### So the minimum waiting time for positioning is equal to $3_{subframe} \times 6 \ \mathrm{s} = 18 \mathrm{s} $

___

# Task 2
## a) 
#### 1 frame: 1500 bits $\implies$ 5 subframes: 300 bits each

## b)
```python
from pathlib import Path

SUB_FRAMES = 5
SUB_FRAMES_BIT_LENGTH = 300
TOTAL_BITS = 1500

# pre-allocation
subframes = [
    [0] * SUB_FRAMES_BIT_LENGTH for _ in range(SUB_FRAMES)
]

subframe_ids = [0] * SUB_FRAMES


def bits_to_int(bits):
    val = 0
    for b in bits:
        val = (val << 1) | b
    return val


with open (Path(__file__).parent / 'NavMessage_ss26.txt', 'r') as file:
    for i in range(TOTAL_BITS):
        bit = int(file.readline().strip())
        current_subframe = i // SUB_FRAMES_BIT_LENGTH
        current_subframe_index = i % SUB_FRAMES_BIT_LENGTH

        subframes[current_subframe][current_subframe_index] = bit

        if current_subframe_index == 52: # 30 (TLM offset) + 22 (HOW offset)
            bits = subframes[current_subframe][49: 52]
            subframe_ids[current_subframe] = bits_to_int(bits)

```

#### Result: $[2, 4, 5, 3, 1]$
